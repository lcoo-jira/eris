---
- hosts: localhost
  tasks:
  - name: Test module
    rally_mod:
      command: "create_deployment"
      command_args: {'name':'deji'}
    register: result
  - debug: var=result

  - name: rally deployment
    rally_deployment:
      command: create

  - name: Task create
    rally_task:
      command: create    
